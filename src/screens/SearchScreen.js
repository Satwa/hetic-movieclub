import React, { useState, useEffect } from 'react'
import { Text, View, Image, StyleSheet, Button, Linkedin, Animated } from 'react-native'

export default SearchScreen = props => {
	return (
		<View>
			<Text>Search</Text>
		</View>
	)
}
